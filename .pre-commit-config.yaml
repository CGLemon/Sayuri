repos:
  - repo: https://github.com/pre-commit/mirrors-clang-format
    rev: v18.1.8
    hooks:
      - id: clang-format
        name: clang-format
        files: ^(src|test)/.*\.(cc|cpp|h|hpp)$
        exclude: ^(third_party|build|target)/
